pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = flumotion-transcoder.pc

noinst_DATA = flumotion-transcoder.pc.in

all-local: flumotion-transcoder-uninstalled.pc

EXTRA_DIST = flumotion-transcoder.pc.in flumotion-transcoder-uninstalled.pc.in
