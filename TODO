- Add the result of the "file" command in the report
- Add the file header (hexdump) in the report
- Make each transcoding log in separate files to be able to add it in the error report
- Make the file monitor build a configuration for each file with htaccess-like files
- Add a parameter to acknowledge to say if and where the source file should be moved
  to be able to make inplace transcoding (without moving the sourcefile).